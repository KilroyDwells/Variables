var1 = 120
var2 = var1
print(var2)

#esta suma retornada en float

numer1 = 10
numer2 = 9.99
numer3 = numer1 + numer2
print(numer3)
type(numer3)

primer_numero = 7
segundo_numero = 9
suma = primer_numero + segundo_numero
print(suma)
primera_cadena = "Saludos"
segunda_cadena =  "11"
respuesta = primera_cadena + segunda_cadena
print(respuesta)

var1 = 10
var2 = 30
var3 = 180
var4 = 500

maximo = max(var1, var2, var3, var4)
print(maximo)

minimo = min(var1, var2, var3, var4)
print(minimo)

sum((var1, var2, var3, var4))

#Crear una función
def func(parametro1, parametro2)
    #codigo de la funcion <-- Identicación o sangría
    return 

#llamar la funcion
func(argumento,argumento)
#Almacenar el retorno de la función (si tiene uno):
variable = func(argumento, argumento)

def repite_frases():
    imprime_frases()
    imprime_frases()

def imprime_frases():
    print("La clase es fantastica")
    print("Python es increible") 
    
imprime_frases()

def repite_frases():
    imprime_frases()
    imprime_frases()
    
repite_frases()
